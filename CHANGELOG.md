# Changelog

## Unreleased
- Security hardening (gitignore + pre-commit hooks)
- Docs system (runs + repo map)
- CI policies (lint + README update enforcement)
- Agent instruction files (AGENTS.md, CLAUDE.md, .claude/settings.json)

## v0.1.0
- Initial umbrella repo scaffold
- Stage 1 dataset builder (traces → trials/features parquet + QC plots)
